import Dialog from './Dialog.js';

export default class AboutDialog extends Dialog {
  /**
   * @param {DialogProps} props
   */
  constructor (props) {
    super(props);

    this._render();
  }

  _render () {
  }
}
